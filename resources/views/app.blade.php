<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>QHS Inventory unavailable</title>
    <style>
        body { margin: 0; min-height: 100vh; display: grid; place-items: center; font: 16px/1.5 system-ui, sans-serif; color: #262326; background: #f7f5f6; }
        main { width: min(34rem, calc(100% - 2rem)); padding: 2rem; border: 1px solid #e4dde0; border-radius: 1rem; background: white; box-shadow: 0 1rem 3rem rgba(65, 31, 43, .08); }
        h1 { margin-top: 0; color: #741b32; }
        code { padding: .15rem .35rem; border-radius: .25rem; background: #f3eef0; }
    </style>
</head>
<body>
<main>
    <h1>Frontend build not found</h1>
    <p>Install and build the React application with <code>npm --prefix QHS install</code> and <code>npm --prefix QHS run build</code>.</p>
</main>
</body>
</html>
